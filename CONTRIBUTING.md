# Contributing

1. Follow the architecture described in `docs/architecture.md` and `docs/how-to-add-module.md`.
2. Commits must follow [Conventional Commits](https://www.conventionalcommits.org/) (enforced by commitlint).
3. Run `npm run lint && npm run typecheck && npm run test` before opening a PR.
